const CONSTANT = {
  TIME_REPEAT_AGAIN: 24 * 60 * 60, // 24h chạy 1 lần
  PROJECT_REPEAT: ['cats-small', 'goats', 'bool', 'ducks', 'duck-chain'], // Hằng ngày sẽ chạy những con này
};

module.exports = CONSTANT